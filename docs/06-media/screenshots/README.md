# Screenshots

Capture checklist (run the app, then save PNGs here). Each should be retina (2×), dark theme.

- [ ] `dashboard.png` — project list (and the designed empty state)
- [ ] `project-detail.png` — tabs: Vision · PRD · Activity
- [ ] `artifact-editor.png` — editor + version history + AI panel
- [ ] `command-palette.png` — ⌘K fuzzy search (when built)
- [ ] `generation-streaming.png` — live AI generation with token/cost ticker

> Until the UI is elevated to the Pro Max standard, these should be re-captured after the UI slice.
